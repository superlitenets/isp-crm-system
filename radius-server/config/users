# User authorization file
# Users are authenticated against the SQL database
# Unknown user handling is done in the site config using unlang
# This file is intentionally empty to avoid conflicts with SQL auth
