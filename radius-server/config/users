# User authorization file
# Users are authenticated against the SQL database
# This DEFAULT entry catches unknown users and accepts them with expired pool

# Catch-all for unknown users - MUST be at bottom
DEFAULT Auth-Type := Accept
    Framed-Pool := "expired-pool",
    Mikrotik-Rate-Limit := "128k/128k",
    Mikrotik-Group := "expired",
    Session-Timeout := 300,
    Acct-Interim-Interval := 60,
    Reply-Message := "Account not found - redirecting to portal"
